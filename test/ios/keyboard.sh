#!/bin/bash

# Manually via ~/Library/Preferences/com.apple.iphonesimulator.plist

# Disable default Connect Hardware Keyboard settings on simulator
defaults write com.apple.iphonesimulator ConnectHardwareKeyboard -bool no
